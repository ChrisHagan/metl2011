<flyouts:FlyoutCard x:Class="SandRibbon.Components.ContentVisibility"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:api="clr-namespace:SandRibbon;assembly=SandRibbonInterop"
    xmlns:dict="clr-namespace:SandRibbon.Components.ResourceDictionaries"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sr="clr-namespace:SandRibbon"
    xmlns:flyouts="clr-namespace:SandRibbon.Frame.Flyouts"
    xmlns:layout="clr-namespace:SandRibbon.Layout">
    <Grid layout:GridLayout.ItemsPerRow="1">
        <TextBlock Text="You see" Style="{StaticResource subHeading}" Foreground="Black"/>
        <ItemsControl x:Name="contentVisibilitySelectors">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <CheckBox ToolTip="{Binding Description}" Content="{Binding Label}" Foreground="Black">
                        <CheckBox.IsChecked>
                            <Binding Path="Subscribed" Mode="TwoWay" NotifyOnTargetUpdated="False" NotifyOnSourceUpdated="True" UpdateSourceTrigger="PropertyChanged"></Binding>
                        </CheckBox.IsChecked>
                    </CheckBox>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</flyouts:FlyoutCard>
