<Window x:Class="SandRibbon.Components.Submissions.ViewSubmissions"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:SandRibbon="clr-namespace:SandRibbon"
    xmlns:api="clr-namespace:SandRibbon;assembly=SandRibbonInterop"
    Title="View Submissions"
    SizeToContent="WidthAndHeight">
    <Window.CommandBindings>
        <CommandBinding Command="{x:Static api:Commands.ImportSubmission}" Executed="importSubmission" CanExecute="canImportSubmission"/>
    </Window.CommandBindings>
    <Border Background="{StaticResource BackgroundBlueGradient}">
        <StackPanel Orientation="Vertical" >
            <StackPanel Orientation="Horizontal">
                <ScrollViewer>
                    <ListBox x:Name="submissions" SelectionMode="Single" Background ="Transparent" MaxHeight="600">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel MaxWidth="300"></WrapPanel>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Viewbox Width="100" Height="100">
                                    <Image Source="{Binding Path=url, Converter={x:Static SandRibbon:Converters.ConvertStringToImageSource}}" ToolTip="{Binding Path=author}"/>
                                </Viewbox>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </ScrollViewer>
                <StackPanel Orientation="Vertical">
                    <Label>Preview</Label>
                    <Image x:Name="previewImage" Source="{Binding ElementName=submissions, Path=SelectedItem, 
                        Converter={x:Static SandRibbon:Converters.ExtractUrlAndConvertConverter}}" />
                </StackPanel>
            </StackPanel>
            <Button Command="{x:Static api:Commands.ImportSubmission}" HorizontalAlignment="Right">Import selected submission</Button>
        </StackPanel>
    </Border>
</Window>
