<Window x:Class="SandRibbon.Components.Submissions.ViewSubmissions"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:SandRibbon="clr-namespace:SandRibbon"
    xmlns:api="clr-namespace:SandRibbon;assembly=SandRibbonInterop"
    Topmost="True"
    Title="View Submissions"
    x:Name="viewSubmissions"
    SizeToContent="WidthAndHeight">
    <Window.CommandBindings>
        <CommandBinding Command="{x:Static api:Commands.ImportSubmission}" Executed="importSubmission" CanExecute="canImportSubmission"/>
    </Window.CommandBindings>
    <Border Background="{StaticResource BackgroundBlueGradient}">
        <DockPanel>
            <Button DockPanel.Dock="Bottom" x:Name="importSelectedSubmission" ToolTip="{DynamicResource DisplaySubmissionToolTip}" Command="{x:Static api:Commands.ImportSubmission}" HorizontalAlignment="Right">Display submission</Button>
            <Grid MaxHeight="450">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition MaxWidth="325"/>
                    <ColumnDefinition MaxWidth="325"/>
                    <ColumnDefinition MinWidth="500"/>
                </Grid.ColumnDefinitions>
                <ListBox Grid.Column="0" x:Name="Buckets" SelectionMode="Single" Background="Transparent" SelectedIndex="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled" ItemsSource="{Binding Path=submissionList}" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Path=Count, Converter={x:Static SandRibbon:Converters.submissionCountConverter}}"></TextBlock>
                                <TextBlock Margin="10 0 0 0 " Text="{Binding Path=From}"/>
                                <TextBlock Margin="10 0 0 0 " Text="{Binding Path=To}"/>
                            </StackPanel>
                                
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <ListBox Grid.Column="1" x:Name="submissions" SelectionMode="Single" Background ="Transparent" 
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ItemsSource="{Binding ElementName=Buckets, Path=SelectedItem.submissions}" >
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Viewbox Width="100" Height="100">
                                <Image Source="{Binding Path=url, Converter={x:Static SandRibbon:Converters.ConvertStringToImageSource}}" />
                            </Viewbox>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <Grid x:Name="GridContainer" Grid.Column="2" Width="Auto" Height="Auto" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                    <Label Height="35" HorizontalAlignment="Center" VerticalAlignment="Top" 
                           Content="{Binding 
                                        ElementName=submissions, 
                                        Path=SelectedItem,
                                        Converter={x:Static SandRibbon:Converters.ExtractSubmissionAuthor}}"/>
                    <Viewbox Margin="0,35,0,0" Height="Auto" Width="{Binding ElementName=GridContainer, Path=ActualWidth}" VerticalAlignment="Top" HorizontalAlignment="Left" >
                        <Image x:Name="previewImage" Source="{Binding ElementName=submissions, Path=SelectedItem, 
                        Converter={x:Static SandRibbon:Converters.ExtractUrlAndConvertConverter}}" Stretch="UniformToFill"/>
                    </Viewbox>
                </Grid>
            </Grid>
        </DockPanel>
    </Border>
</Window>
