<UserControl x:Class="SandRibbon.Components.Submissions.ScreenshotSubmission"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:api="clr-namespace:SandRibbon;assembly=SandRibbonInterop"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" >
    <StackPanel Orientation="Horizontal">
        <Button x:Name="hiddenButton" Visibility="Collapsed" Command="api:Commands.DummyCommandToProcessCanExecute" Tag="This button should always be collapsed.  It is only being used to redirect a canExecute Handler."/>
        <Button x:Name="submitSubmission" Click="generateScreenshot" IsEnabled="{Binding ElementName=hiddenButton, Path=IsEnabled}">
            <TextBlock>Submit a screenshot</TextBlock>
        </Button>
        <Button x:Name="viewSubmission" Click="viewSubmissions" IsEnabled="{Binding ElementName=hiddenButton, Path=IsEnabled}">
            <TextBlock>View Submissions</TextBlock>
        </Button>
    </StackPanel>
</UserControl>
