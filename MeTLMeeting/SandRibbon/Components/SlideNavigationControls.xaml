<UserControl x:Class="SandRibbon.Components.SlideNavigationControls"
    xmlns:SandRibbonInterop="clr-namespace:SandRibbonInterop;assembly=SandRibbonInterop"
    xmlns:api="clr-namespace:SandRibbon;assembly=SandRibbonInterop"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:quizzing="clr-namespace:SandRibbon.Quizzing"
    Height="{Binding ElementName=nav, Path=ActualHeight}"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <quizzing:GoldPlaque Height="Auto"/>
        <Rectangle HorizontalAlignment="Stretch" Width="Auto" Height="Auto" VerticalAlignment="Stretch" Fill="{StaticResource ShinyHighlightBrush}" RadiusX="3" RadiusY="3" />
        <SandRibbonInterop:RibbonPanel Orientation="Horizontal" Visibility="Hidden" x:Name="nav" HorizontalAlignment="Center" Margin="0 0 0 0">
            <SandRibbonInterop:Button Text="Previous" Icon="\Resources\Previous.png" x:Name="moveToPrevious" ToolTip="{StaticResource PreviousSlideToolTip}" Command="{x:Static api:Commands.MoveToPrevious}" />
            <SandRibbonInterop:Button Text="Sync" Icon="\Resources\SyncGreen.png" x:Name="syncButton" ToolTip="{StaticResource SyncToolTip}" Click="toggleSync"/>
            <SandRibbonInterop:Button Text="Add Page" Icon="/Resources/NewSlide.png" ToolTip="{StaticResource AddSlideToolTip}" x:Name="addSlideButton"  Command="{x:Static api:Commands.AddSlide}"/>
            <SandRibbonInterop:Button Text="Next" Icon="\Resources\Next.png" x:Name="moveToNext" ToolTip="{StaticResource NextSlideToolTip}" Command="{x:Static api:Commands.MoveToNext}" />
        </SandRibbonInterop:RibbonPanel>
    </Grid>
</UserControl>