<Window x:Class="SandRibbon.Components.DiagnosticWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SandRibbon.Components"
        mc:Ignorable="d"
        x:Name="diagnosticWindow"
        Title="MeTL Diagnostics" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
         <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <DataGrid x:Name="messages" Grid.Row="1" Grid.Column="0" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="message" Binding="{Binding message}"/>
                <DataGridTextColumn Header="category" Binding="{Binding category}"/>
                <DataGridTextColumn Header="when" Binding="{Binding when}"/>
            </DataGrid.Columns>
        </DataGrid>
        <DataGrid x:Name="gauges" Grid.Row="1" Grid.Column="1" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="name" Binding="{Binding GaugeName}"/>
                <DataGridTextColumn Header="category" Binding="{Binding GaugeCategory}"/>
                <DataGridTextColumn Header="status" Binding="{Binding status}"/>
                <DataGridTextColumn Header="value" Binding="{Binding value}"/>
                <DataGridTextColumn Header="started" Binding="{Binding started}"/>
                <DataGridTextColumn Header="finished" Binding="{Binding finished}"/>
                <DataGridTextColumn Header="duration" Binding="{Binding duration}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
