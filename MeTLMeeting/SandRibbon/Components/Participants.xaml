<UserControl x:Class="SandRibbon.Components.Participants"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:components="clr-namespace:SandRibbon.Components"
             xmlns:sr="clr-namespace:SandRibbon">
    <StackPanel>
        <Button Content="Add new group" Click="AddGroup"/>
        <Button Content="Clear groups" Click="ClearGroups"/>
        <ScrollViewer>
            <DataGrid x:Name="participantListBox" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Groups">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ListBox ItemsSource="{Binding Path=Membership}">
                                    <ListBox.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" />
                                        </ItemsPanelTemplate>
                                    </ListBox.ItemsPanel>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox Click="CheckBox_Click" IsChecked="{Binding Path=IsMember}"/>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn IsReadOnly="True" Header="User" Binding="{Binding Path=username, Mode=OneWay}" />
                    <DataGridTextColumn IsReadOnly="True" Header="Activity" Binding="{Binding Path=activityCount, Mode=OneWay}" />
                    <DataGridTextColumn IsReadOnly="True" Header="Submissions" Binding="{Binding Path=submissionCount, Mode=OneWay}" />
                    <DataGridTextColumn IsReadOnly="True" Header="Slides" Binding="{Binding Path=slideLocation, Converter={x:Static components:Participants.slideCollectionDescriber}, Mode=OneWay}"/>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </StackPanel>
</UserControl>
