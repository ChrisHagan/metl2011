<UserControl x:Class="SandRibbon.Components.Sandpit.S15Boards"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:api="clr-namespace:SandRibbon;assembly=SandRibbonInterop"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Button Command="{x:Static api:Commands.WakeUp}" CommandParameter="S15">
            <TextBlock Text="Wake up!" />
        </Button>
        <Popup x:Name="boardSelector" PopupAnimation="Fade" Placement="Bottom" AllowsTransparency="True">
            <Border BorderBrush="DarkGray" Background="{StaticResource BackgroundBlueGradient}" Padding="4" CornerRadius="4">
            <Grid Height="420" Width="400">
                    <Button HorizontalAlignment="Right" VerticalAlignment="Top" Click="ClosePopup">Close BoardManager</Button>
                        <Image x:Name="RoomBackground" Source="../../Resources/S15.png" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Height="400" Width="400"/>
                <ItemsControl x:Name="boardDisplay">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Width="400" Height="400" IsItemsHost="True" />
                        </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding Path=x}" />
                        <Setter Property="Canvas.Top" Value="{Binding Path=y}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" BorderBrush="Black" BorderThickness="2" CornerRadius="2" Padding="2"><Button>
                            <TextBlock Text="{Binding Path=name}" />
                        </Button></Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
                </Grid>
                </Border>
        </Popup>
    </Grid>
</UserControl>
