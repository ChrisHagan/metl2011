<Page x:Class="SandRibbon.Pages.Conversations.ConversationSearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:sr="clr-namespace:SandRibbon"
    xmlns:ribbon="clr-namespace:Microsoft.Windows.Controls.Ribbon;assembly=RibbonControlsLibrary"            
    xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:components="clr-namespace:SandRibbon.Components"      
    xmlns:providers="clr-namespace:SandRibbon.Providers"
    xmlns:core="clr-namespace:System;assembly=mscorlib"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dict="clr-namespace:SandRibbon.Components.ResourceDictionaries"
    xmlns:api="clr-namespace:SandRibbon;assembly=SandRibbonInterop"
    xmlns:macros="clr-namespace:SandRibbon.Pages.Macros"
    xmlns:layout="clr-namespace:SandRibbon.Layout"
    xmlns:conversation="clr-namespace:SandRibbon.Pages.Conversations"
    xmlns:p="clr-namespace:SandRibbon.Properties"
      xmlns:ppType="clr-namespace:SandRibbon.Utils"
    x:Name="root">
    <Page.Resources>
        <Thickness Top="50" x:Key="space"/>
        <conversation:VisibleToAuthor x:Key="visibleToAuthor" />
    </Page.Resources>
    <Grid Height="Auto" Width="Auto" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid layout:GridLayout.ItemsPerRow="1" Grid.Row="0">
            <Label Content="What are you looking for?" Margin="{StaticResource clearBottom}"/>
            <TextBox x:Name="SearchInput" PreviewKeyUp="OnPreviewKeyUp" MaxLength="50"
                 Margin="{StaticResource space}"
                 Controls:TextBoxHelper.Watermark="Search for a conversation by person or title"/>
            <StackPanel Orientation="Horizontal">
                <Label Style="{StaticResource medium}" Content="Only my conversations" />
                <CheckBox x:Name="onlyMyConversations" Checked="onlyMyConversations_Checked" 
                          Unchecked="onlyMyConversations_Checked"/>
            </StackPanel>
            <Button Click="searchConversations_Click">
                <Label Content="Search" />
            </Button>
            <UniformGrid HorizontalAlignment="Left" Columns="2">
                <Button Click="AnalyzeSelectedConversations">
                    <Label Content="Analyze selected conversations" Style="{StaticResource medium}" />
                </Button>
                <Button Click="SynchronizeToOneNote">
                    <Label Content="Send selected conversations to OneNote" Style="{StaticResource medium}" />
                </Button>
            </UniformGrid>
            <UniformGrid HorizontalAlignment="Left" Columns="2">
                <Button Command="{x:Static api:Commands.CreateBlankConversation}">
                    <Label Content="Create new conversation" Style="{StaticResource medium}"/>
                </Button>
                <!--<Button Command="{x:Static api:Commands.ImportPowerpoint}" CommandParameter="{x:Static ppType:PowerpointImportType.Image}">-->
                    <Button Command="{x:Static api:Commands.ImportPowerpoint}" CommandParameter="{x:Static ppType:PowerPointLoader.IMAGE}">
                        <Label Content="Import conversation from powerpoint" Style="{StaticResource medium}"/>
                </Button>
                <Button Command="{x:Static api:Commands.ImportPowerpoint}" CommandParameter="{x:Static ppType:PowerPointLoader.SHAPES}">
                    <Label Content="Import conversation from powerpoint (Flexible)" Style="{StaticResource medium}"/>
                </Button>
                <Button Command="{x:Static api:Commands.ImportPowerpoint}" CommandParameter="{x:Static ppType:PowerPointLoader.HIGHDEFIMAGE}">
                    <Label Content="Import conversation from powerpoint (High Quality)" Style="{StaticResource medium}"/>
                </Button>
            </UniformGrid>
        </Grid>
        <DataGrid x:Name="SearchResults" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                  Grid.Row="1"
                  Margin="{StaticResource space}" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Enter conversation" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="JoinConversation">
                                <Button.Background>
                                    <SolidColorBrush Color="{DynamicResource AccentColor}" />
                                </Button.Background>
                                <Viewbox Width="{DynamicResource ButtonWidth}" Height="{DynamicResource ButtonHeight}">
                                    <macros:IconControl Content="appbar_door_enter" />
                                </Viewbox>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Title" Binding="{Binding Path=Title}" />
                <DataGridTextColumn Header="Owner" Binding="{Binding Path=Author}" />
                <DataGridTextColumn Header="Slides" Binding="{Binding Path=Slides.Count}" />
                <DataGridTextColumn Header="Created" Binding="{Binding Path=Created}" />
                <DataGridTextColumn Header="Identity" Binding="{Binding Path=Jid}" />
                <DataGridTemplateColumn Header="Edit" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="EditConversation" Visibility="{Binding Converter={StaticResource visibleToAuthor}}">
                                <Rectangle Width="20" Height="20">
                                    <Rectangle.Fill>
                                        <VisualBrush Visual="{StaticResource appbar_cog}" />
                                    </Rectangle.Fill>
                                </Rectangle>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
